# ── Core framework ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── Database (PostgreSQL — run history & project configs) ─────────────────────
psycopg2-binary>=2.9.0
sqlalchemy>=2.0          # NIA DB engine for per-org LLM config lookup
cachetools>=5.3          # TTL cache for per-org dynamic config

# ── Async HTTP ───────────────────────────────────────────────────────────────
aiohttp>=3.9.0

# ── LLM routing ─────────────────────────────────────────────────────────────
litellm>=1.30.0

# ── LLM Provider SDKs ────────────────────────────────────────────────────────
groq>=0.4.0
google-generativeai>=0.4.0
openai>=1.12.0          # Azure OpenAI

# ── Evaluation: LLM quality frameworks ───────────────────────────────────────
deepeval>=0.21.0
ragas>=0.1.0
langchain-openai>=0.1.0     # required by RAGAS for Azure OpenAI LLM wrapper
langchain>=0.1.0            # required by ragas internally

# ── Security: PII detection ───────────────────────────────────────────────────
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
# spaCy model required by Presidio — install separately:
#   python -m spacy download en_core_web_lg

# ── Security: toxicity classifier ────────────────────────────────────────────
detoxify>=0.5.0

# ── Security: prompt injection detection ─────────────────────────────────────
llm-guard>=0.3.0

# ── Load testing ─────────────────────────────────────────────────────────────
locust>=2.20.0

# ── Golden datasets (AdvBench, HarmBench from HuggingFace) ───────────────────
datasets>=2.14.0

# ── NLP support ───────────────────────────────────────────────────────────────
# spacy version managed by presidio-analyzer (requires spacy~=3.4 or 3.5, not 3.7)
# Do NOT pin spacy here — let presidio pull its compatible version automatically
torch>=2.0.0            # required by detoxify
sentence-transformers>=2.6.0   # cosine similarity for RAG answer correctness

# ── Auth: password hashing + JWT ─────────────────────────────────────────────
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# ── AWS SDK (SES email notifications) ────────────────────────────────────────
boto3>=1.34.0

# ── Optional: document parsing ───────────────────────────────────────────────
# PyPDF2>=3.0.0
